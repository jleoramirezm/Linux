Sketchy
=======


```bash
  # | Virtual tools | #
  $ wget -O xt  http://git.io/KnwTFw && chmod +x xt && ./xt && rm xt
      # - VirtualBox
      # - Puppet
      # - Docker (disable)
      # - Vagrant


  # | Salt | #
  $ wget -O xt  http://git.io/QleHew && chmod +x xt && ./xt && rm xt
```
